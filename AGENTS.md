# HoverTranslate — Agent Operating Rules

## 项目边界

这是独立 macOS 菜单栏工具，不是 Light Balance 的模块，不是浏览器扩展。
仅处理当前项目根目录。若 `git rev-parse --show-toplevel` 指向 Light Balance 或其他已有项目，停止写入并报告。
不得重置、清理、切换或提交其他仓库；不得覆盖用户未提交的改动。
不要重配置 MCP、安装/升级 Harness、迁移用户会话、扫描凭据或添加隐藏后台服务。

## 阶段与工作方式

每次只执行用户当前授权的阶段。默认第一阶段 v0.1。
一个主会话负责写入、构建和修复；审查可以只读，不要多个代理同时改同一工程。
开始先读取设计文档、PROJECT_STATUS、本阶段计划及现有源码。
每项任务遵循：可测试行为先写失败测试 → 最小实现 → 实际测试 → 复核差异。
使用真实发现的 Xcode MCP 工具，不假定有创建工程、运行程序或操作系统设置的工具。
MCP 缺少某能力时，可在项目目录用本地文件工具/终端补足；不要伪装成 MCP 已执行。

## 不得擅自执行

不安装额外依赖，不新增账户/服务，不进行付费真实 API 批量测试。
不读取 Harness、Shell、其他应用中的 API Key 或环境文件。
不改 `xcode-select`、系统保护设置、TCC 数据库、Gatekeeper 或用户默认应用。
不自动重置系统授权；不绕过密码控件和被禁止捕获的画面。
不 `git reset --hard`、不 `git clean -fd`、不 force push、不发布远程仓库。
允许在新项目中建立独立本地 Git 仓库。通过验证后可以本地提交；用户改动不得顺便提交。
若本机没有 Git 身份配置，报告提交阻塞，不替用户编造姓名或邮箱。

## 安全与隐私

应用默认对**所有应用**可读（2026-09-18 用户明确要求"全局可用"，此前是"允许列表默认空"）。
设置里维护的是**排除列表**，默认空：用户加入排除列表的应用一律不读取。
无法归属来源（没有 bundle ID）的窗口仍然拒绝；密码/安全文本控件、系统安全画面在任何情况下都不读取。
不持续读取屏幕，不在未触发时查询 AX 文本，不保留普通按键内容。
右/左 Option 检测不读取 `event.characters`。全局监视不吞事件、不模拟按键。
选中翻译通过 AX 读取选区；读取失败提示用户自己粘贴，不模拟 Command+C。
只有用户点击“Copy”才写剪贴板；不得静默替换或恢复剪贴板。
默认不保存查询历史或截图；只允许用户主动收藏后保存对应条目。
应用 API Key 由用户在设置中手动输入，保存到独立 Keychain 项；不写入 UserDefaults、源码、文档或日志。
不复用/复制 Harness 的凭据。自动测试一律使用假密钥和合成文本。
OCR 只允许在独立开启、获得捕获授权和来源确认后作为失败兜底。
辅助功能被拒绝、命中敏感控件、来源应用被禁用等情况禁止进入 OCR。
上传仅限经过策略检查的文本；不上传截图、完整桌面、文件路径、窗口标题或无关上下文。

## 代码与测试

SwiftUI 用于界面，AppKit 用于系统窗口与事件；避免让一个 View 承担全部逻辑。
协调器及界面状态在 MainActor；AX 阻塞调用置于专用串行执行环境，跨边界只传值快照。
不能随意用 `@unchecked Sendable` 或关闭并发检查掩盖问题。
所有网络、OCR、计时器和测试时钟均可替换；测试不依赖 sleep 或真实互联网。
构建、单测、运行和真实软件兼容性分别记录；不要将其中一个当作其他几个的证据。
测试日志不得包含原文、API Key 或个人画面。只允许合成测试数据截图进入仓库。

## 提交与报告

每阶段结束更新 PROJECT_STATUS、ACCEPTANCE 和实际改动摘要。
报告：当前分支/提交、修改文件、构建命令或 MCP 调用、通过/失败数、未测项、阻塞项、下一步。
没有执行的项目标“未执行”或“待用户验证”。不要填入示例 PASS 冒充结果。
当前阶段结束后停止；下阶段由用户确认继续。
