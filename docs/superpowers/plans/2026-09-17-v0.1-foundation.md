# HoverTranslate v0.1 — 取词基础 Implementation Plan

> **For agentic workers:** 若当前环境已提供 superpowers，使用 `superpowers:executing-plans` 按任务执行；可独立审查的任务可使用 `superpowers:subagent-driven-development`，但本项目默认单主会话、串行写入。未安装这些技能时按本文件逐项执行，不为满足技能名称擅自安装插件。以 `- [ ]` 跟踪真实完成状态。

> **2026-09-18 更新（当前规则）**：本计划写于"允许列表默认空"时期。来源策略已在 2026-09-18 按用户要求反转为
> **排除列表默认空**——默认**所有应用**都可读，设置里只能逐个排除；无法归属来源与密码/安全控件仍然拒绝。
> 以 `AGENTS.md` 与设计文档 §4 为准；本文中出现"允许列表"的地方属于当时的历史记录，不再是当前规则。


**Goal:** 按住 Option 悬停时准确显示英文原文，证明核心取词链路可行。

**Architecture:** 先建立纯逻辑与界面壳，再增加串行 AX 提取，最后连接非激活浮窗；没有任何云端请求或屏幕截图。

**Tech Stack:** Swift、SwiftUI、AppKit、ApplicationServices；按阶段加入 Foundation URLSession、Security、NaturalLanguage、ScreenCaptureKit 和 Vision。

**Spec:** `docs/superpowers/specs/2026-09-17-hovertranslate-design.md`

## Global Constraints

- 独立 macOS 原生项目，暂名 HoverTranslate；不属于 Light Balance。
- Swift + SwiftUI + AppKit；部署目标设计为 macOS 14.0，必须由本机 SDK 核对可用性。
- 第一轮只执行 v0.1；每个版本提交实测报告后停止，不自动跨版本。
- 默认右 Option 悬停；设置提供左右 Option 选择；不吞掉或重放原应用事件。
- 仅按用户明确触发读取；应用允许列表默认空；不默认截图、联网或保存原文。
- 不碰 Light Balance、Harness 配置、系统安全策略或其他项目。
- 使用当前发现的 Xcode MCP 工具；不得猜测工具名、参数或已完成操作。
- 普通日志不得包含原文、译文、截图、密钥、HTTP 请求体或响应体。
- 测试使用合成数据；构建成功不代表跨应用交互通过。
- 所有时间、上限和阈值都是设计初值，不是已经测出的性能。

---

## 任务 1：原生工程、允许列表与触发策略

**Files**
- Create: `HoverTranslate/App/HoverTranslateApp.swift`
- Create: `HoverTranslate/App/AppDelegate.swift`
- Create: `HoverTranslate/Core/TriggerPolicy.swift`
- Create: `HoverTranslate/Input/TriggerController.swift`
- Create: `HoverTranslate/Settings/SettingsStore.swift`
- Test: `HoverTranslateTests/TriggerPolicyTests.swift`

**Interfaces**
- Consumes: 本机按键状态、功能开关、允许列表；不读取普通键盘字符串。
- Produces: `QueryMode`（word、sentence、selection、explanation）；`TriggerPolicy.mode(rightOption:leftOption:shift:enabled:useRightOption:) -> QueryMode?`。
- v0.1 只允许 word；其他模式保留枚举名称供后续，不能提前显示无功能入口。

- [ ] 核对独立目录、实际 MCP 工具、Xcode版本；建立/读取工程和测试 target。部署目标设计 14.0，签名配置记录到状态文档。
- [ ] 写入以下失败测试，不用真实全局键盘事件驱动单测。

```swift
import XCTest
@testable import HoverTranslate

final class TriggerPolicyTests: XCTestCase {
    func testLeftOptionDoesNotTriggerRightOptionMode() {
        XCTAssertNil(TriggerPolicy.mode(rightOption: false,
            leftOption: true, shift: false, enabled: true,
            useRightOption: true))
    }
    func testReleaseRightOptionStopsEvenIfLeftStillHeld() {
        XCTAssertEqual(TriggerPolicy.mode(rightOption: true,
            leftOption: true, shift: false, enabled: true,
            useRightOption: true), .word)
        XCTAssertNil(TriggerPolicy.mode(rightOption: false,
            leftOption: true, shift: false, enabled: true,
            useRightOption: true))
    }
    func testDisabledNeverTriggers() {
        XCTAssertNil(TriggerPolicy.mode(rightOption: true,
            leftOption: false, shift: false, enabled: false,
            useRightOption: true))
    }
}
```

- [ ] 用总计划的 test 命令追加 `-only-testing:HoverTranslateTests/TriggerPolicyTests`，确认实际失败原因。
- [ ] 实现最小策略：启用且选定一侧 Option 为按下时返回 word，否则 nil；Shift 在本阶段不升级句子。
- [ ] 实现必要的 local/global event 观察与清理；左右键依据公开键位常量和实际状态核对，不写未说明来源的位掩码。
- [ ] 增加250ms等待、4点稳定半径；计时可注入，松键/禁用立即撤销。AppKit回调不做AX/网络阻塞工作。[S3]
- [ ] 创建菜单栏 Enable/Settings/Quit。允许列表默认空；辅助功能申请只在用户点击功能时引导，不自动勾选。
- [ ] 重跑测试并真实构建；手动验证左右同时按住、松开一个、切换应用、退出后无遗留监视。

**验收**：菜单栏可见、可退出；未允许的应用不会读取；键盘输入不被拦截；与目标软件快捷键冲突如实登记。

## 任务 2：精确 AX 取词与坐标/索引契约

**Files**
- Create: `HoverTranslate/Core/TextResolver.swift`
- Create: `HoverTranslate/Core/CoordinateMapper.swift`
- Create: `HoverTranslate/Core/ExtractionSnapshot.swift`
- Create: `HoverTranslate/Extraction/AccessibilityTextExtractor.swift`
- Test: `HoverTranslateTests/TextResolverTests.swift`
- Test: `HoverTranslateTests/CoordinateMapperTests.swift`

**Interfaces**
- Consumes: 鼠标坐标、源应用身份和generation。
- Produces: 设计文档定义的ExtractionSnapshot，或明确失败原因。
- 纯函数：`TextResolver.word(in: String, atUTF16Offset: Int) -> String?`。
- 坐标：`CoordinateMapper.appKitToAX(_: CGPoint, primaryFrame: CGRect) -> CGPoint`。

- [ ] 写入文本测试，明确 UTF-16 偏移不是 Swift 字符下标。

```swift
import XCTest
@testable import HoverTranslate

final class TextResolverTests: XCTestCase {
    func testEmojiBeforeWordDoesNotShiftHit() {
        let text = "🙂 Open Settings."
        let offset = (text as NSString).range(of: "Settings").location + 2
        XCTAssertEqual(TextResolver.word(in: text,
            atUTF16Offset: offset), "Settings")
    }
    func testWhitespaceIsNotNearestWord() {
        XCTAssertNil(TextResolver.word(in: "Open Settings", atUTF16Offset: 4))
    }
    func testInvalidOffsetIsSafe() {
        XCTAssertNil(TextResolver.word(in: "Open", atUTF16Offset: -1))
        XCTAssertNil(TextResolver.word(in: "Open", atUTF16Offset: 99))
    }
}

final class CoordinateMapperTests: XCTestCase {
    func testMainDisplayReferenceAlsoWorksForDisplayAbove() {
        let primary = CGRect(x: 0, y: 0, width: 1440, height: 900)
        XCTAssertEqual(CoordinateMapper.appKitToAX(
            CGPoint(x: 100, y: 800), primaryFrame: primary),
            CGPoint(x: 100, y: 100))
        XCTAssertEqual(CoordinateMapper.appKitToAX(
            CGPoint(x: -100, y: 1000), primaryFrame: primary),
            CGPoint(x: -100, y: -100))
    }
}
```

- [ ] 运行上述两个测试类，记录真实红灯。
- [ ] 实现有界英文词解析：支持词内撇号/连字符；空白、纯标点和非法索引返回nil。不要对整文按空格平均分配屏幕位置。
- [ ] 将 AppKit 全局点与 AX 左上基准的转换独立实现；主屏基准不能误用“当前key window所在屏幕”，Retina缩放不参与点到点转换。[S1]
- [ ] 在串行环境中创建/使用AX对象；按位置取元素→核对PID/角色/策略→查询位置范围→读取有界文字→确认词框覆盖鼠标。[S1][S2]
- [ ] 不支持精确范围时只可返回有界短标签或明确失败，不读取整个文档；敏感控件直接退出。
- [ ] 将值快照返回主协调器；记录 source/scope，不输出原文日志。
- [ ] 重跑文本/坐标测试与构建；在TextEdit英文内容和一个系统按钮上实测。未支持的软件记失败而非继续改成猜测取词。

**验收**：鼠标指向 Settings 不得返回整页或Open；空白不翻邻词；按钮整体只能标label；emoji前缀不导致错位。

## 任务 3：请求失效与原文浮窗

**Files**
- Create: `HoverTranslate/Core/RequestGate.swift`
- Create: `HoverTranslate/Core/TranslationCoordinator.swift`
- Create: `HoverTranslate/Presentation/FloatingPanelController.swift`
- Create: `HoverTranslate/Presentation/TranslationCard.swift`
- Test: `HoverTranslateTests/RequestGateTests.swift`
- Update: `docs/PROJECT_STATUS.md`、`docs/ACCEPTANCE.md`

**Interfaces**
- `RequestGate.begin() -> UInt64` 开始新代次。
- `RequestGate.invalidate()` 使当前代次失效。
- `RequestGate.accepts(_ id: UInt64) -> Bool` 判断结果是否仍可显示。
- 协调器消费ExtractionSnapshot；v0.1只展示原文，不实例化翻译网络服务。

- [ ] 写入失败测试。

```swift
import XCTest
@testable import HoverTranslate

final class RequestGateTests: XCTestCase {
    func testLateFirstResultCannotOverwriteSecond() {
        var gate = RequestGate()
        let first = gate.begin()
        let second = gate.begin()
        XCTAssertFalse(gate.accepts(first))
        XCTAssertTrue(gate.accepts(second))
    }
    func testReleaseInvalidatesOutstandingWork() {
        var gate = RequestGate()
        let current = gate.begin()
        gate.invalidate()
        XCTAssertFalse(gate.accepts(current))
    }
}
```

- [ ] 运行RequestGateTests，确认红灯；实现递增代次和有效性，加入假提取器乱序返回的协调器测试。
- [ ] 每个回调先检查generation、触发状态与来源策略，再更新界面；cancel只是第一层，不替代检查。
- [ ] 使用NSPanel非激活样式和SwiftUI内容；不要默认调用activate/makeKeyAndOrderFront。[S4]
- [ ] 依据设计文档实现按住时穿透、松键冻结后可点击、Pin固定、Close恢复查询；固定卡片不能被悬停覆盖。
- [ ] 根据鼠标所在屏幕的visibleFrame安排卡片，自动翻边且不遮盖命中词；不要只取NSScreen.main。
- [ ] 真实测试换词、滚动、切换应用、松键迟到、输入框继续输入；同时跑全套单测和构建。
- [ ] 填写验收A组和真实软件矩阵，检查git diff；只在验证后本地提交，不自动进入v0.2。

**本阶段结束必须能演示**：按住键→看到准确原文→松键→卡片按规则冻结/关闭；不会调用API，也不会申请截图权限。
